workload: Interactive-BI
operation: delete
number: 5
title: Remove forum membership
description: |
  Given a *Forum* with ID `$forumId` and a *Person* with ID `$personId`, remove the *hasMember* edge between them.
parameters:
  - name: forumId
    type: ID
  - name: personId
    type: ID
choke_points: [9.4]
relevance:
  n/a
