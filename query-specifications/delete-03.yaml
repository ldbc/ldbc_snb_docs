workload: Updates
operation: delete
number: 3
title: Remove comment like
description: |
  Given a *Person* with ID `$personId` and a *Comment* with ID `$commentId`, remove the *likes* edge between them.
parameters:
  - name: personId
    type: ID
  - name: commentId
    type: ID
choke_points: [9.4]
relevance:
  Removal of a *likes* edge is a rare event, e.g. people accidently liking a *Comment*, this can be reflected by the relative frequency of the operation.
