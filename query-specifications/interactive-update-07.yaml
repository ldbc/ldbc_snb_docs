workload: Interactive
operation: update
number: 7
title: Add Comment
description: Add a *Comment* **node** replying to a *Post*/*Comment*, connected to the network by 5 possible **edge** types (*replyOf*, *hasCreator*, *containerOf*, *isLocatedIn*, *hasTag*).
parameters:
  - name: Comment.id
    type: ID
  - name: Comment.creationDate
    type: DateTime
  - name: Comment.locationIp
    type: String
  - name: Comment.browserUsed
    type: String
  - name: Comment.content
    type: Text
  - name: Comment.length
    type: 32-bit Integer
  - name: Comment-hasCreator->Person.id
    type: ID
  - name: Comment-isLocatedIn->Country.id
    type: ID
  - name: Comment-replyOf->Post.id
    type: ID
    category: calculated
    description: $-1$ if the *Comment* is a reply of a *Comment*
  - name: Comment-replyOf->Comment.id
    type: ID
    category: calculated
    description: $-1$ if the *Comment* is a reply of a *Post*
  - name: Comment-hasTag->Tag.id
    type: \{ID\}
