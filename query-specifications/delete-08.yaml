workload: Updates
operation: delete
number: 8
title: Remove friendship
description:
  Given two *Person* nodes with IDs `$person1Id` and `$person2Id`, remove the *knows* edge between them.
parameters:
  - name: person1Id
    type: ID
  - name: person2Id
    type: ID
choke_points: [9.4]
relevance:
  n/a
