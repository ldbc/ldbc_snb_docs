workload: Interactive
operation: delete
number: 8
title: Remove friendship
description:
  Given two *Person* nodes, remove the *knows* edge between them.
parameters:
  - name: Person.id
    type: ID
    description: "`person1Id`"
  - name: Person.id
    type: ID
    description: "`person2Id`"
relevance:
  n/a
