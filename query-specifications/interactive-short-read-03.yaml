workload: Interactive / short
number: 3
title: Person Friends
description: |
  Given a start Person, retrieve all of their friends, and the date at which they became friends.
parameters:
  - name: Person.id
    type: ID
result:
  - name: Person.id
    type: ID
  - name: Person.firstName
    type: String
  - name: Person.lastName
    type: String
  - name: Knows.creationDate
    type: DateTime
sort:
  - name: Knows.creationDate
    direction: desc
  - name: Person.id
    direction: asc
