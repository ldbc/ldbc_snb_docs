workload: Updates
operation: delete
number: 7
title: Remove comment subthread
description: |
  Remove a *Comment* node with ID `$commentId` and its **edges** (*isLocatedIn*, *likes*, *hasCreator*, *hasTag*).
  In addition, remove all replies to the *Comment* connected by *replyOf* and their **edges**.
parameters:
  - name: commentId
    type: ID
choke_points: [9.3, 9.4, 9.5]
relevance:
  n/a
