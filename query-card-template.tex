\renewcommand*{\arraystretch}{1.1}

\noindent\begin{tabularx}{17cm}{|p{1.95cm}|X|}
	\hline
	workload    & {{ workload }} \\ \hline
%
	query       & {{ number }} \\ \hline
%
	title       & {{ title }} \\ \hline
	\multicolumn{2}{|c|}{ \includegraphics[scale=\patternscale,margin=0cm .2cm]{patterns/{{ query_name -}} }} \\ \hline
	description & {{ description }} \\ \hline
	{% if group %}
%
	group by       &
	\multicolumn{1}{>{\raggedright}X|}{
		{% for g in group -%}
			\varname{ {{- g -}} }{% if not loop.last %}, {% endif %}
		{% endfor -%}
	} \\ \hline
	{% endif %}
%
	parameters  &
	\vspace{1.1ex}{\begin{tabularx}{14.38cm}{|c|M|m{2cm}|Y} \hline
	{% for entry in parameters -%}
	\cellcolor{black!70} \color{white} $\mathsf{ {{- loop.index -}} }$ & \varname{ {{- entry.name -}} } & \cellcolor{gray!20} \vartype{ {{- entry.type -}} } & {{ entry.description }} \\ {%- if not loop.last %}\hline{% endif %}
	{% endfor -%}
	\end{tabularx}} \\ \hline
%
	result      &
	\vspace{1.1ex}{\begin{tabularx}{14.38cm}{|c|M|m{2cm}|Y} \hline
	{% for entry in result -%}
	\cellcolor{black!70} \color{white} $\mathsf{ {{- loop.index -}} }$ & \varname{ {{- entry.name -}} } & \cellcolor{gray!20} \vartype{ {{- entry.type -}} } & {{ entry.description }} \\ {%- if not loop.last %}\hline{% endif %}
	{% endfor -%}
	\end{tabularx}} \\ \hline
	{% if sort -%}
%
	sort        &
	\vspace{1.1ex}{\begin{tabular}{|c|l|c|} \hline
	{% for entry in sort -%}
	\cellcolor{black!70} \color{white} $\mathsf{ {{- loop.index -}} }$ & \varname{ {{- entry.name -}} } & \cellcolor{gray!20} $\{{- entry.direction -}}$ \\ {%- if not loop.last %}\hline{% endif %}
	{% endfor -%}
	\end{tabular}} \\ \hline
	{% endif -%}
%
	{% if limit -%}
	limit       & {{ limit }} \\ \hline
	{% endif -%}
%
	choke points &
	\multicolumn{1}{>{\raggedright}X|}{
		{% for choke_point in choke_points -%}
			\chokepoint{ {{- choke_point -}} } {%- if not loop.last %}, {% endif %}
		{% endfor -%}
	}\\ \hline
\end{tabularx}
\clearpage
