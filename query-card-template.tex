\renewcommand*{\arraystretch}{1.5}
\begin{tabularx}{15cm}{|p{2.1cm}@{\hskip 1ex}|@{\hskip 1ex}X|}
	\hline
	number      & {{ number }}                                                          \\ \hline
	title       & {{ title }}                                                           \\ \hline
	\multicolumn{2}{|c|}{ \includegraphics[scale=\patternscale,margin=0cm .2cm]{patterns/q {{- number_string -}} }} \\ \hline
	description & {{ description }} \\ \hline
	{% if group %}
	group by       &
	\multicolumn{1}{>{\raggedright}X|}{
		{% for g in group -%}
			\varname{ {{- g -}} }{% if not loop.last %}, {% endif %}
		{% endfor -%}
	}\\ \hline
	{% endif %}
	parameters  &
	\multicolumn{1}{>{\raggedright}X|}{
		{% for entry in parameters -%}
			\variable{ {{- entry.name -}} }{ {{- entry.type -}} } {% if not loop.last %}\\{% endif %}
		{% endfor -%}
	}\\ \hline
	result      &
	\multicolumn{1}{>{\raggedright}X|}{
		{% for entry in result -%}
			\variable{ {{- entry.name -}} }{ {{- entry.type -}} } {{- entry.description -}} {% if not loop.last %}\\{% endif %}
		{% endfor -%}
	}\\ \hline
	{% if sort -%}
	sort        &
	\multicolumn{1}{>{\raggedright}X|}{
		{% for entry in sort -%}
			\sortentry{ {{- entry.name -}} }{\{{- entry.direction -}} } {{- entry.description -}}  {% if not loop.last %}\\{% endif %}
		{% endfor -%}
	}\\ \hline
	{% endif -%}
	{% if limit -%}
	limit       & {{ limit }}                                                           \\ \hline
	{% endif -%}
	choke points        &
	\multicolumn{1}{>{\raggedright}X|}{
		{% for choke_point in choke_points -%}
			\chokepoint{ {{- choke_point -}} } {%- if not loop.last %}, {% endif %}
		{% endfor -%}
	}\\ \hline
\end{tabularx}
\clearpage
