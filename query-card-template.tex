\renewcommand*{\arraystretch}{1.1}

\noindent\begin{tabularx}{17cm}{|>{\small \sf}c|X|}
	\hline
	query    & {{ workload }} / {{ number }} \\ \hline
%
	title       & {{ title }} \\ \hline
%
    pattern     & \hfill\includegraphics[scale=\patternscale,margin=0cm .2cm]{patterns/{{ query_name -}} }\hfill\vadjust{} \\ \hline
%
	desc. & {{ description }} \\ \hline
%
	{% if group %}
	group by       &
	\multicolumn{1}{>{\raggedright}X|}{
		{% for g in group -%}
			\varname{ {{- g -}} }{% if not loop.last %}, {% endif %}
		{% endfor -%}
	} \\ \hline
	{% endif %}
%
	params.  &
	\vspace{1.1ex}{\begin{tabularx}{14.2cm}{|c|M|m{2cm}|Y|} \hline
	{% for entry in parameters -%}
	\cellcolor{parameter} \color{white} $\mathsf{ {{- loop.index -}} }$ & \varname{ {{- entry.name -}} } & \cellcolor{gray!20} \vartype{ {{- entry.type -}} } & {{ entry.description }} \\ \hline
	{% endfor -%}
	\end{tabularx}}\vspace{1.1ex} \\ \hline
%
	{% if result %}
	result      &
	\vspace{1.1ex}{\begin{tabularx}{14.2cm}{|c|M|m{2cm}|c|Y|} \hline
	{% for entry in result -%}
	\cellcolor{result} \color{white} $\mathsf{ {{- loop.index -}} }$ & \varname{ {{- entry.name -}} } & \cellcolor{gray!20} \vartype{ {{- entry.type -}} } &
	    \texttt{
	    {%- if   entry.category == 'aggregated' -%} A
	    {%- elif entry.category == 'calculated' -%} C
	    {%- else -%} R
	    {%- endif -%}
	    } &
	    {{ entry.description }} \\ \hline
	{% endfor -%}
	\end{tabularx}}\vspace{1.1ex} \\ \hline
	{% endif %}
%
	{% if sort -%}
	sort        &
	\vspace{1.1ex}{\begin{tabular}{|c|l|c|} \hline
	{% for entry in sort -%}
	\cellcolor{sort} \color{white} $\mathsf{ {{- loop.index -}} }$ & \varname{ {{- entry.name -}} } & \cellcolor{gray!20} $\{{- entry.direction -}}$ \\ \hline
	{% endfor -%}
	\end{tabular}}\vspace{1.1ex} \\ \hline
	{% endif -%}
%
	{% if limit -%}
	limit       & {{ limit }} \\ \hline
	{% endif -%}
%
	{% if choke_points -%}
	CPs &
	\multicolumn{1}{>{\raggedright}l|}{
	  {% for choke_point in choke_points -%}
	    \chokepoint{ {{- choke_point -}} } {%- if not loop.last %}, {% endif %}
	  {% endfor -%}
	} \\ \hline
	{% endif -%}
%
    {% if relevance -%}
    relevance &
      \small {{ relevance }} \\ \hline
    {%- endif -%}
%
\end{tabularx}
\vspace{2ex}
